[package]
name = "ray-compiler-rust"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2.12.0", default-features = false, features = ["napi4", "serde-json"] }
napi-derive = "2.12.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
